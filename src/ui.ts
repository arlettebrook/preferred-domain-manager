import { escapeHtml } from "./validation";

export function landingPage(host: string) {
  return `<!doctype html><html lang="zh-CN"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>优选域名管理面板</title><style>
body{margin:0;background:#08111f;color:#edf3ff;font:16px system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif}main{max-width:760px;margin:16vh auto;padding:24px}.card{border:1px solid #263754;background:#121d31;border-radius:18px;padding:32px;box-shadow:0 20px 60px #0004}a{color:#78a8ff}small{color:#96a5bd}
</style></head><body><main><div class="card"><h1>优选域名管理面板</h1><p>欢迎访问 <strong>${escapeHtml(host)}</strong>。此页面由 Cloudflare Worker 提供。</p><p><a href="/admin">进入 DNS 管理后台 →</a></p><small>根域名与泛域名均可绑定到本 Worker。</small></div></main></body></html>`;
}

export function adminPage() {
  return String.raw`<!doctype html><html lang="zh-CN"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>优选域名管理面板</title><style>
:root{color-scheme:dark;--bg:#08111f;--panel:#101b2d;--panel-strong:#14233a;--text:#edf3ff;--muted:#91a2bc;--border:#263956;--primary:#5d7df5;--primary-hover:#7d96ff;--danger:#ed6b82;--ok:#45d09b;--warning:#e6b85c;--shadow:0 18px 45px #0003}body.light{color-scheme:light;--bg:#f3f6fb;--panel:#fff;--panel-strong:#edf3fb;--text:#14213a;--muted:#637089;--border:#d7e0ed;--primary:#326ed2;--primary-hover:#1e5bbd;--danger:#c6374d;--ok:#138a61;--warning:#9a6b00;--shadow:0 12px 28px #34507818}*{box-sizing:border-box}body{margin:0;background:var(--bg);color:var(--text);font:14px system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif}button,input,textarea,select{font:inherit}button{border:0;border-radius:9px;padding:10px 14px;color:#fff;background:var(--primary);cursor:pointer;transition:background .15s,transform .15s,opacity .15s}button:hover{background:var(--primary-hover)}button:active{transform:translateY(1px)}button.secondary{color:var(--text);background:var(--panel-strong);border:1px solid var(--border)}button.danger{background:var(--danger)}button:disabled{opacity:.55;cursor:not-allowed;transform:none}input,textarea,select{width:100%;color:var(--text);background:var(--bg);border:1px solid var(--border);border-radius:9px;padding:10px 11px;outline:none}input:focus,textarea:focus,select:focus{border-color:var(--primary);box-shadow:0 0 0 3px #5d7df533}input[readonly]{color:var(--muted);background:var(--panel-strong)}textarea{min-height:116px;resize:vertical}label{display:block;margin:13px 0 6px;color:var(--muted);font-size:13px}.hidden{display:none!important}.login{max-width:420px;margin:16vh auto}.login h2{margin-top:0}.layout{display:grid;grid-template-columns:238px minmax(0,1fr);min-height:100vh}.sidebar{background:#0d1728;border-right:1px solid var(--border);padding:22px 14px}.brand{font-size:18px;font-weight:800;padding:10px 14px 30px;letter-spacing:.2px}.brand span{display:inline-flex;align-items:center;justify-content:center;width:32px;height:32px;border-radius:9px;background:linear-gradient(135deg,#4b6fff,#744dff);margin-right:9px}.nav{display:grid;gap:6px}.nav button{display:flex;align-items:center;gap:8px;width:100%;text-align:left;background:transparent;color:#9fafc9;padding:13px 14px;font-size:15px}.nav button.active,.nav button:hover{background:#182b50;color:#fff}.nav-icon{display:inline-block;width:22px;color:#9fb7e7}.content{min-width:0}.topbar{height:76px;padding:0 34px;border-bottom:1px solid var(--border);display:flex;align-items:center;justify-content:space-between;background:var(--panel)}.topbar h1{font-size:23px;margin:0}.top-actions{display:flex;gap:8px}.main{max-width:1440px;width:100%;padding:30px 34px;margin:auto}.page{display:none}.page.active{display:block}.page-head{display:flex;justify-content:space-between;align-items:flex-start;gap:15px;margin-bottom:20px}.page-head h2{margin:0 0 6px;font-size:23px}.page-head p{margin:0;color:var(--muted)}.dashboard-grid{display:grid;grid-template-columns:minmax(0,1.35fr) minmax(320px,.65fr);gap:18px}.span-all{grid-column:1/-1}.card{background:var(--panel);border:1px solid var(--border);border-radius:15px;padding:20px;box-shadow:var(--shadow)}.card h3{margin:0 0 7px;font-size:17px}.card p{color:var(--muted);line-height:1.6;margin:7px 0 14px}.section-head{display:flex;align-items:flex-start;justify-content:space-between;gap:15px;margin-bottom:14px}.section-head h3{margin:0}.section-head p{margin:4px 0 0;font-size:12px}.row{display:flex;gap:12px;align-items:flex-end;flex-wrap:wrap}.row>*{flex:1;min-width:180px}.actions{display:flex;gap:8px;align-items:center;flex-wrap:wrap}.status{min-height:22px;color:var(--muted);line-height:1.5}.ok{color:var(--ok)}.err{color:var(--danger)}.warning{color:var(--warning)}.hint{font-size:12px;color:var(--muted)}.source{display:grid;grid-template-columns:minmax(0,1fr) auto auto;gap:8px;align-items:center;margin:8px 0}.source label{margin:0;white-space:nowrap}.metrics{display:grid;grid-template-columns:repeat(3,1fr);gap:10px;margin-top:16px}.metric{padding:14px;border:1px solid var(--border);border-radius:11px;background:var(--panel-strong)}.metric strong{display:block;font-size:23px;margin-bottom:4px}.metric span{color:var(--muted);font-size:12px}.output{margin:15px 0 0!important;max-height:220px;overflow:auto;white-space:pre-wrap;word-break:break-word;background:var(--bg);font:12px ui-monospace,SFMono-Regular,Consolas,monospace}.toolbar{display:flex;gap:10px;align-items:center;justify-content:space-between;margin-bottom:14px}.toolbar-left{display:flex;align-items:center;gap:10px;min-width:0;flex:1}.toolbar input{max-width:360px}.table-wrap{overflow:auto;border:1px solid var(--border);border-radius:11px}.table{width:100%;border-collapse:collapse;min-width:760px}.table th,.table td{padding:12px;text-align:left;border-bottom:1px solid var(--border);vertical-align:middle}.table th{color:var(--muted);font-size:11px;text-transform:uppercase;letter-spacing:.06em;background:var(--panel-strong)}.table tr:last-child td{border-bottom:0}.record-name{font-weight:650}.record-content{max-width:340px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;font-family:ui-monospace,SFMono-Regular,Consolas,monospace;font-size:12px}.badge{display:inline-block;border:1px solid var(--border);border-radius:20px;padding:3px 8px;color:var(--muted);font-size:11px;white-space:nowrap}.badge.managed{color:#a9baff;border-color:#526fd1}.empty{text-align:center;color:var(--muted);padding:40px}.dialog{border:1px solid var(--border);border-radius:15px;background:var(--panel);color:var(--text);padding:0;max-width:530px;width:calc(100% - 30px);box-shadow:var(--shadow)}.dialog::backdrop{background:#020611aa}.dialog-body{padding:22px}.dialog-head{display:flex;justify-content:space-between;align-items:center;gap:10px}.dialog-head h3{margin:0;font-size:19px}.close{background:transparent;color:var(--muted);font-size:21px;padding:4px 8px}.dialog-actions{display:flex;justify-content:flex-end;gap:8px;margin-top:18px}.check{display:flex;align-items:center;gap:8px;color:var(--text);margin:13px 0}.check input{width:auto}.sr-only{position:absolute;width:1px;height:1px;padding:0;margin:-1px;overflow:hidden;clip:rect(0,0,0,0);white-space:nowrap;border:0}@media(max-width:980px){.layout{grid-template-columns:1fr}.sidebar{position:static;padding:10px;border-right:0;border-bottom:1px solid var(--border)}.brand{padding:7px 10px 12px}.nav{display:flex;overflow:auto}.nav button{white-space:nowrap;width:auto}.main{padding:22px 18px}.dashboard-grid{grid-template-columns:1fr}.span-all{grid-column:auto}.topbar{padding:0 18px}.topbar h1{font-size:20px}}@media(max-width:620px){.page-head,.section-head,.toolbar{display:block}.page-head .actions,.section-head .actions,.toolbar .actions{margin-top:12px}.toolbar-left{margin-bottom:10px}.toolbar input{max-width:none}.row>*{min-width:100%}.source{grid-template-columns:1fr auto}.source .remove-source{grid-column:2;grid-row:1}.metrics{grid-template-columns:1fr}.card{padding:16px}.top-actions button{padding:8px 10px}.table{min-width:700px}}
</style></head><body>
<div id="login" class="login card"><h2>管理员登录</h2><p>使用 Worker Secret <code>ADMIN_PASSWORD</code> 登录。</p><form id="loginForm"><label for="password">管理员密码</label><input id="password" type="password" autocomplete="current-password" required><div class="actions" style="margin-top:14px"><button type="submit">登录</button><span id="loginStatus" class="status"></span></div></form></div>
<div id="app" class="layout hidden"><aside class="sidebar"><div class="brand"><span>优</span>优选域名管理面板</div><nav class="nav"><button type="button" data-page="dashboard" class="active"><span class="nav-icon">▦</span>仪表盘</button><button type="button" data-page="settings"><span class="nav-icon">⚙</span>全局设置</button></nav></aside><div class="content"><header class="topbar"><h1 id="pageTitle">仪表盘</h1><div class="top-actions"><button type="button" id="theme" class="secondary">☀️/🌙</button><button type="button" id="logout" class="secondary">退出</button></div></header><main class="main">
<section id="dashboard" class="page active"><div class="page-head"><div><h2>仪表盘</h2><p>集中查看优选 IP、DNS 记录和运行状态。</p></div><div class="status" id="domainStatus"></div></div><div class="dashboard-grid">
<section class="card span-all"><div class="section-head"><div><h3>Cloudflare 连接</h3><p>这些设置保存到 KV，并用于 DNS 编辑与优选 IP 同步。Token 只显示配置状态。</p></div><span id="runtimeStatus" class="status"></span></div><div class="row"><div><label for="defaultDomain">默认域名</label><input id="defaultDomain" placeholder="example.com" autocomplete="off"></div><div><label for="cfZoneId">Cloudflare Zone ID</label><input id="cfZoneId" placeholder="Cloudflare Zone ID" autocomplete="off"></div><div><label for="cfApiToken">Cloudflare API Token</label><input id="cfApiToken" type="password" placeholder="留空保持已有 Token" autocomplete="off"></div></div><div class="actions" style="margin-top:14px"><button type="button" id="saveRuntime">保存连接设置</button><span class="hint">API Token 至少需要 Zone Read 和 DNS Edit 权限。</span></div><div class="metrics"><div class="metric"><strong id="dnsCount">—</strong><span>可管理 DNS 记录</span></div><div class="metric"><strong id="sourceCount">—</strong><span>已配置 IP 来源</span></div><div class="metric"><strong id="manualCount">—</strong><span>手动 IP 数量</span></div></div></section>
<section class="card"><div class="section-head"><div><h3>IP 来源</h3><p>支持 JSON、纯文本及嵌套数组。</p></div></div><div id="sources"></div><button type="button" id="addSource" class="secondary">＋ 添加来源</button></section>
<section class="card"><div class="section-head"><div><h3>手动优选 IP</h3><p>每行一个 IPv4 或 IPv6，会与来源结果合并去重。</p></div></div><textarea id="manualIps" placeholder="1.1.1.1&#10;2606:4700:4700::1111"></textarea></section>
<section class="card span-all"><div class="section-head"><div><h3>优选 IP 操作</h3><p>同步前会检测 TCP 443；没有通过项时不会改动现有 DNS。</p></div></div><div class="actions"><button type="button" id="saveConfig">保存全部配置</button><button type="button" id="preview" class="secondary">检测优选 IP</button><button type="button" id="sync">立即同步 DNS</button><span id="status" class="status"></span></div><pre id="output" class="card output">等待操作…</pre></section>
<section class="card span-all"><div class="section-head"><div><h3>DNS 编辑</h3><p>这里只展示默认域名的 A、AAAA、CNAME；保存后会自动同步对应泛记录。</p></div><div class="actions"><button type="button" id="refreshDns" class="secondary">刷新</button><button type="button" id="addDns">＋ 新建记录</button></div></div><div class="toolbar"><div class="toolbar-left"><label class="sr-only" for="dnsFilter">搜索 DNS 记录</label><input id="dnsFilter" placeholder="搜索类型、内容或名称"><span id="dnsStatus" class="status"></span></div></div><div class="table-wrap"><table class="table"><thead><tr><th>类型</th><th>名称</th><th>内容</th><th>TTL</th><th>代理</th><th>来源</th><th>操作</th></tr></thead><tbody id="dnsRows"><tr><td colspan="7" class="empty">正在加载 DNS 记录…</td></tr></tbody></table></div></section>
</div></section>
<section id="settings" class="page"><div class="page-head"><div><h2>全局设置</h2><p>配置 Telegram Bot，并将设置安全保存到 KV。</p></div></div><div class="card"><h3>Telegram Bot</h3><p>通过 Telegram 查看、添加、修改和删除默认域名记录，泛记录会自动同步。只有白名单用户可以操作。</p><label for="telegramBotToken">Bot Token</label><input id="telegramBotToken" type="password" autocomplete="off" placeholder="留空保持已有 Token"><label for="telegramWebhookSecret">Webhook Secret</label><input id="telegramWebhookSecret" type="password" autocomplete="off" placeholder="留空保持已有 Secret"><label for="telegramAllowedUserIds">允许的 Telegram 用户 ID</label><textarea id="telegramAllowedUserIds" placeholder="例如：123456789&#10;每行一个，也支持逗号或空格分隔"></textarea><p id="telegramConfigStatus" class="hint"></p><div class="actions"><button type="button" id="saveTelegram">保存 Telegram 设置</button><button type="button" id="telegramTest" class="secondary">测试 Bot</button><button type="button" id="telegramSetWebhook" class="secondary">设置 Webhook</button><button type="button" id="telegramDeleteWebhook" class="danger">删除 Webhook</button><button type="button" id="telegramCommands" class="secondary">同步菜单命令</button><span id="telegramStatus" class="status"></span></div><p class="hint">Webhook 地址为当前 Worker 域名下的 <code>/telegram/webhook</code>。请先保存 Token、Secret 和用户 ID。</p></div></section>
</main></div></div><dialog id="dnsDialog" class="dialog"><form id="dnsForm"><div class="dialog-body"><div class="dialog-head"><h3 id="dnsDialogTitle">新建 DNS 记录</h3><button type="button" id="closeDns" class="close" aria-label="关闭">×</button></div><label for="dnsType">记录类型</label><select id="dnsType"><option>A</option><option>AAAA</option><option>CNAME</option></select><p id="dnsRuleHint" class="hint">名称固定为默认域名；保存后会自动同步对应泛记录。</p><label for="dnsName">主域名</label><input id="dnsName" readonly required><label for="dnsContent">内容</label><textarea id="dnsContent" placeholder="记录内容" required></textarea><div class="row"><div><label for="dnsTtl">TTL</label><input id="dnsTtl" value="最低（60 秒）" disabled></div><label class="check"><input id="dnsProxied" type="checkbox" disabled>固定 DNS only（灰云）</label></div><div class="dialog-actions"><button type="button" id="cancelDns" class="secondary">取消</button><button type="submit" id="saveDns">保存记录</button></div></div></form></dialog><script>
const $=selector=>document.querySelector(selector);
const state={ipSources:[],manualIps:[],defaultDomain:'',cfZoneId:'',hasCfApiToken:false,telegramAllowedUserIds:[],hasTelegramBotToken:false,hasTelegramWebhookSecret:false};
const dnsState={records:[],editingId:null,loading:false};
const mobileFix=document.createElement('style');mobileFix.textContent='@media(max-width:620px){.source{grid-template-columns:minmax(0,1fr) auto auto}.source .remove-source{grid-column:auto;grid-row:auto}}';document.head.appendChild(mobileFix);
const esc=value=>String(value??'').replace(/[&<>"']/g,character=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[character]));
function setStatus(element,text,bad=false){if(!element)return;element.textContent=text;element.className='status '+(bad?'err':'ok');}
function setBusy(selector,busy){const button=$(selector);if(button)button.disabled=busy;}
async function api(path,options={}){const request={...options,headers:{...(options.body?{'content-type':'application/json'}:{}),...(options.headers||{})}};const response=await fetch(path,request);const body=await response.json().catch(()=>({}));if(response.status===401&&path!=='/api/auth/login'){const wasLoggedIn=!$('#app').classList.contains('hidden');$('#app').classList.add('hidden');$('#login').classList.remove('hidden');throw Error(wasLoggedIn?'登录已过期，请重新登录':'未登录');}if(!response.ok)throw Error(body.error||('HTTP '+response.status));return body;}
function formatTtl(ttl){return ttl===60?'最低（60 秒）':ttl==null?'—':String(ttl)+' 秒';}
function syncDnsName(){const name=$('#dnsName');if(name)name.value=state.defaultDomain||'';}
function renderConfig(){const domain=$('#defaultDomain');if(domain)domain.value=state.defaultDomain||'';const zone=$('#cfZoneId');if(zone)zone.value=state.cfZoneId||'';const token=$('#cfApiToken');if(token){token.value='';token.placeholder=state.hasCfApiToken?'已配置 Token（留空保持）':'尚未配置 Token';}const bot=$('#telegramBotToken');if(bot){bot.value='';bot.placeholder=state.hasTelegramBotToken?'已配置 Token（留空保持）':'尚未配置 Bot Token';}const secret=$('#telegramWebhookSecret');if(secret){secret.value='';secret.placeholder=state.hasTelegramWebhookSecret?'已配置 Secret（留空保持）':'尚未配置 Webhook Secret';}const ids=$('#telegramAllowedUserIds');if(ids)ids.value=(state.telegramAllowedUserIds||[]).join('\n');const configStatus=$('#telegramConfigStatus');if(configStatus)configStatus.textContent='Bot Token：'+(state.hasTelegramBotToken?'已配置':'未配置')+' · Webhook Secret：'+(state.hasTelegramWebhookSecret?'已配置':'未配置')+' · 白名单：'+(state.telegramAllowedUserIds||[]).length+' 个用户';const sources=$('#sources');if(sources)sources.innerHTML=state.ipSources.map((source,index)=>'<div class="source" data-index="'+index+'"><input class="s-url" placeholder="https://…" value="'+esc(source.url)+'"><label><input class="s-enabled" type="checkbox" '+(source.enabled?'checked':'')+'> 启用</label><button type="button" class="danger remove-source">删除</button></div>').join('');const manual=$('#manualIps');if(manual)manual.value=state.manualIps.join('\n');const sourceCount=$('#sourceCount');if(sourceCount)sourceCount.textContent=String(state.ipSources.length);const manualCount=$('#manualCount');if(manualCount)manualCount.textContent=String(state.manualIps.length);syncDnsName();const domainStatus=$('#domainStatus');if(domainStatus)domainStatus.textContent=state.defaultDomain?'当前域名：'+state.defaultDomain:'尚未配置默认域名';}
function readConfig(){state.defaultDomain=$('#defaultDomain').value;state.cfZoneId=$('#cfZoneId').value;state.cfApiToken=$('#cfApiToken').value||undefined;state.telegramBotToken=$('#telegramBotToken').value||undefined;state.telegramWebhookSecret=$('#telegramWebhookSecret').value||undefined;state.telegramAllowedUserIds=$('#telegramAllowedUserIds').value.split(/[\s,]+/).map(value=>value.trim()).filter(value=>/^\d+$/.test(value)).slice(0,50);state.ipSources=[...document.querySelectorAll('.source')].map((element,index)=>({...state.ipSources[index],url:element.querySelector('.s-url').value,enabled:element.querySelector('.s-enabled').checked}));state.manualIps=$('#manualIps').value.split(/[\s,]+/).map(value=>value.trim()).filter(Boolean);}
async function persistConfig(){readConfig();Object.assign(state,await api('/api/config',{method:'PUT',body:JSON.stringify(state)}));renderConfig();}
async function saveConfig(element,label){try{setStatus(element,label+'中…');await persistConfig();setStatus(element,label+'成功');await loadDns();}catch(error){setStatus(element,error instanceof Error?error.message:String(error),true);}}
async function loadConfig(rethrow=false){try{Object.assign(state,await api('/api/config'));renderConfig();$('#login').classList.add('hidden');$('#app').classList.remove('hidden');await loadDns();}catch(error){const message=error instanceof Error?error.message:String(error);if(message.includes('登录已过期')||message==='未登录')setStatus($('#loginStatus'),message,true);else setStatus($('#runtimeStatus'),message,true);if(rethrow)throw error;}}
function showPage(page){document.querySelectorAll('.page').forEach(element=>element.classList.toggle('active',element.id===page));document.querySelectorAll('.nav button').forEach(element=>element.classList.toggle('active',element.dataset.page===page));$('#pageTitle').textContent=page==='settings'?'全局设置':'仪表盘';if(page==='dashboard')loadDns();}
function updateDnsHint(){const hint=$('#dnsRuleHint');if(hint)hint.textContent=$('#dnsType').value==='CNAME'?'CNAME 会同步创建泛 CNAME，并清理两侧同名的 A/AAAA。':'保存后会同步创建或更新对应类型的泛记录；可用类型：A、AAAA、CNAME。';}
function renderDns(){const filter=($('#dnsFilter').value||'').trim().toLowerCase();const records=dnsState.records.filter(record=>[record.type,record.name,record.content].join(' ').toLowerCase().includes(filter));const rows=$('#dnsRows');if(!rows)return;if(!records.length){rows.innerHTML='<tr><td colspan="7" class="empty">'+(filter?'没有匹配的 DNS 记录':'暂无可管理的 DNS 记录')+'</td></tr>';}else{rows.innerHTML=records.map(record=>'<tr><td><span class="badge">'+esc(record.type)+'</span></td><td class="record-name">'+esc(record.name)+'</td><td class="record-content" title="'+esc(record.content)+'">'+esc(record.content)+'</td><td>'+esc(formatTtl(record.ttl))+'</td><td>'+(record.proxied?'橙云':'灰云')+'</td><td>'+(record.comment==='preferred-domain-manager'||(record.tags||[]).includes('preferred-domain-manager')?'<span class="badge managed">优选托管</span>':'<span class="badge">手动</span>')+'</td><td><div class="actions"><button type="button" class="secondary edit-dns" data-id="'+esc(record.id)+'">编辑</button><button type="button" class="danger delete-dns" data-id="'+esc(record.id)+'">删除</button></div></td></tr>').join('');}const count=$('#dnsCount');if(count)count.textContent=String(dnsState.records.length);}
async function loadDns(){if(dnsState.loading)return;if(!state.defaultDomain||!state.cfZoneId||!state.hasCfApiToken){dnsState.records=[];renderDns();setStatus($('#dnsStatus'),'请先保存默认域名、Zone ID 和 API Token');return;}dnsState.loading=true;setBusy('#refreshDns',true);try{setStatus($('#dnsStatus'),'加载中…');const result=await api('/api/dns/records');dnsState.records=result.records||[];renderDns();setStatus($('#dnsStatus'),'共 '+dnsState.records.length+' 条主域名记录');}catch(error){dnsState.records=[];renderDns();setStatus($('#dnsStatus'),error instanceof Error?error.message:String(error),true);}finally{dnsState.loading=false;setBusy('#refreshDns',false);}}
function openDns(record){syncDnsName();dnsState.editingId=record?.id||null;$('#dnsDialogTitle').textContent=record?'编辑 DNS 记录':'新建 DNS 记录';$('#dnsType').value=record?.type||'A';$('#dnsContent').value=record?.content||'';$('#dnsTtl').value='最低（60 秒）';updateDnsHint();$('#dnsDialog').showModal();setTimeout(()=>$('#dnsContent').focus(),0);}
function closeDns(){const dialog=$('#dnsDialog');if(dialog.open)dialog.close();dnsState.editingId=null;}
async function submitDns(event){event.preventDefault();const button=$('#saveDns');const editing=Boolean(dnsState.editingId);if(button)button.disabled=true;try{const content=$('#dnsContent').value.trim();if(!content)throw Error('记录内容不能为空');await api('/api/dns/records'+(editing?'/'+encodeURIComponent(dnsState.editingId):''),{method:editing?'PUT':'POST',body:JSON.stringify({type:$('#dnsType').value,name:state.defaultDomain,content,proxied:false})});closeDns();await loadDns();setStatus($('#dnsStatus'),editing?'记录已更新':'记录已创建');}catch(error){setStatus($('#dnsStatus'),error instanceof Error?error.message:String(error),true);}finally{if(button)button.disabled=false;}}
async function deleteDns(id){const record=dnsState.records.find(item=>item.id===id);if(!record||!confirm('确定删除 '+record.type+' '+record.name+' 吗？\n对应泛记录也会同步删除。'))return;try{await api('/api/dns/records/'+encodeURIComponent(id),{method:'DELETE'});await loadDns();setStatus($('#dnsStatus'),'记录已删除');}catch(error){setStatus($('#dnsStatus'),error instanceof Error?error.message:String(error),true);}}
let actionBusy=false;async function runAction(path,label){if(actionBusy)return;actionBusy=true;['#preview','#sync','#saveConfig'].forEach(selector=>setBusy(selector,true));try{setStatus($('#status'),label+'中…');await persistConfig();const result=await api(path,{method:'POST',body:'{}'});$('#output').textContent=JSON.stringify(result,null,2);setStatus($('#status'),result.result?.skippedCname?'检测到 CNAME，已跳过 IP 同步':label+'完成');await loadDns();}catch(error){$('#output').textContent=error instanceof Error?error.message:String(error);setStatus($('#status'),$('#output').textContent,true);}finally{actionBusy=false;['#preview','#sync','#saveConfig'].forEach(selector=>setBusy(selector,false));}}
async function telegramAction(path,method,label){try{setBusy('#telegramTest',true);setBusy('#telegramSetWebhook',true);setBusy('#telegramDeleteWebhook',true);setBusy('#telegramCommands',true);setStatus($('#telegramStatus'),label+'中…');await persistConfig();const result=await api(path,{method,body:'{}'});const detail=result.bot?'：'+(result.bot.username?'@'+result.bot.username:(result.bot.first_name||'连接成功')):result.webhookUrl?'：'+result.webhookUrl:'';setStatus($('#telegramStatus'),label+'成功'+detail);}catch(error){setStatus($('#telegramStatus'),error instanceof Error?error.message:String(error),true);}finally{['#telegramTest','#telegramSetWebhook','#telegramDeleteWebhook','#telegramCommands'].forEach(selector=>setBusy(selector,false));}}
document.querySelectorAll('.nav button').forEach(button=>button.addEventListener('click',()=>showPage(button.dataset.page)));
$('#loginForm').addEventListener('submit',async event=>{event.preventDefault();setStatus($('#loginStatus'),'登录中…');setBusy('#loginForm button[type=submit]',true);try{await api('/api/auth/login',{method:'POST',body:JSON.stringify({password:$('#password').value})});$('#password').value='';await loadConfig(true);}catch(error){setStatus($('#loginStatus'),error instanceof Error?error.message:String(error),true);}finally{setBusy('#loginForm button[type=submit]',false);}});
$('#addSource').addEventListener('click',()=>{readConfig();state.ipSources.push({id:crypto.randomUUID(),url:'',enabled:true});renderConfig();document.querySelector('.source:last-child .s-url')?.focus();});
$('#sources').addEventListener('click',event=>{const button=event.target.closest('.remove-source');if(!button)return;readConfig();const source=button.closest('.source');state.ipSources.splice(Number(source.dataset.index),1);renderConfig();});
$('#saveRuntime').addEventListener('click',()=>saveConfig($('#runtimeStatus'),'保存连接设置'));$('#saveConfig').addEventListener('click',()=>saveConfig($('#status'),'保存配置'));$('#preview').addEventListener('click',()=>runAction('/api/ips/preview','检测'));$('#sync').addEventListener('click',()=>runAction('/api/sync','同步'));$('#refreshDns').addEventListener('click',loadDns);$('#dnsFilter').addEventListener('input',renderDns);$('#dnsType').addEventListener('change',updateDnsHint);$('#addDns').addEventListener('click',()=>openDns());$('#closeDns').addEventListener('click',closeDns);$('#cancelDns').addEventListener('click',closeDns);$('#dnsDialog').addEventListener('cancel',event=>{event.preventDefault();closeDns();});$('#dnsForm').addEventListener('submit',submitDns);
$('#dnsRows').addEventListener('click',event=>{const edit=event.target.closest('.edit-dns');if(edit){const record=dnsState.records.find(item=>item.id===edit.dataset.id);if(record)openDns(record);return;}const remove=event.target.closest('.delete-dns');if(remove)deleteDns(remove.dataset.id);});
$('#saveTelegram').addEventListener('click',()=>saveConfig($('#telegramStatus'),'保存 Telegram 设置'));$('#telegramTest').addEventListener('click',()=>telegramAction('/api/telegram/test','POST','测试 Bot'));$('#telegramSetWebhook').addEventListener('click',()=>telegramAction('/api/telegram/webhook','POST','设置 Webhook'));$('#telegramDeleteWebhook').addEventListener('click',()=>telegramAction('/api/telegram/webhook','DELETE','删除 Webhook'));$('#telegramCommands').addEventListener('click',()=>telegramAction('/api/telegram/commands','POST','同步菜单命令'));
$('#logout').addEventListener('click',async()=>{try{await api('/api/auth/logout',{method:'POST'});}finally{location.reload();}});$('#theme').addEventListener('click',()=>{document.body.classList.toggle('light');try{localStorage.pdmTheme=document.body.classList.contains('light')?'light':'dark';}catch{}});try{if(localStorage.pdmTheme==='light')document.body.classList.add('light');}catch{}loadConfig();
</script></body></html>`;
}
