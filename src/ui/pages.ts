/**
 * Stable UI entry point. The large legacy-compatible templates live separately
 * so the Worker entry and future UI modules stay easy to navigate.
 */
export { landingPage } from "./landing-page";
export { adminPage } from "./admin-page";
